# 深挖方向与协作规范

## 深挖方向（已达成后的开放空间）

### P0：b9 捕捉率 100%（目标3 精进）
- 现状：外部轮询结构上 <100%（实测 1/6≈17%@50ms，同 tick 构造+分叉无法外部捕捉）
- 主推 **A1 = FUN_105caa60 入口 hook**（过滤 vt/ready/btype/+0x155c 后写 b9，理论 100% 捕捉，影响面=已验证 b9 语义）
- 备选：A0（写 N=101 数据常驻，零代码待实机探测）、A3（构造器字节 patch 无过滤）
- 新发现：引擎原生 b9 写者 = 3 构造器条件随机写 `b9=RNG%101<=N`（N=[[[attacker+0x160]+0x30]+0x20]，实机 N=0 不触发）——外部直写 = 把原生概率强制 100%

### P1：筛选管线精进
- 规模筛选：单方 <10 且合计 <30 → 跳过（已在 watcher 实装）
- 类型筛选：写 b9 前读 `[pending+0x58]` btype（S15 定案：BATTLE_TYPE 枚举，11-14=海战 → 跳过）

### P2：FOTS / ROTS 差异重验
- b9 偏移在 FOTS（武家之殇）/ ROTS（武家崛起）是否一致（s2_watch 已声明三版本通用，b9 未验）

### P3：战斗细节（AI 行为研究）
- 枪衾（AI 不主动开阵）、三段击（SquadTaskFireVolley 进入条件）、火矢（最小距离门控）、攻门时机（assault_gate 战术类型）——见 reports/ 与 docs/11

### P4：迁移更多全战
- **三国全面战争**：官方 script_ai_planner = 脚本 AI（显傻）；原生 AI 质量需引擎层（a270 思路，64 位）；目标3 无官方强制加载 → b9 思路迁移
- **战锤3**：同族但 64 位 + 现代保护，方案验证后横向迁移

## 工具开发规范（新增工具时）

1. **docstring 必须**：功能 + 原理 + 用法（参照现有工具）
2. **禁止硬编码路径**：用 `os.path.dirname(os.path.abspath(__file__))` 定位；游戏路径用命令行参数
3. **依赖声明**：公共基座 = `probe_battle_env.py`；第三方依赖仅 numpy（README 声明）
4. **只读优先**：默认只读探测，写内存工具必须显式模式参数（probe/write 分离）
5. **提权**：通过 `_run_elev.py` 启动（UAC 一次）

## 提交规范

- commit message：`做了什么 + 结论`
- 结论更新到对应机制地图 / reports 报告
- 被证伪/改写的结论：标注"已证伪/已修正"，不删除历史

## 群 / 协作

- 群管认领深挖方向后，在对应方向标注负责人
- 建议先读：`docs/40_GOAL3_MECHANISM_MAP.md`（目标3 机制地图）→ `reports/`
